APP_ENV=development
DB_HOST=127.0.0.1
DB_PORT=55433
DB_NAME=project_db
DB_USER=example_app_user
DB_PASSWORD=replace_locally
SSH_HOST=example.invalid
SSH_PORT=22
SSH_USER=example_ssh_user
# Optional SSH_PASSWORD; otherwise tunnel.py prompts, or use the explicit same-password flag.
TEST_EXPECTED_HOST=127.0.0.1
TEST_EXPECTED_PORT=55433
TEST_ALLOW_WRITE=


# Generate a random APP_SECRET locally; never reuse a database password.
APP_SECRET=replace_with_random_secret_at_least_32_characters
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
