# SMTP 實信測試（2026-09-12）

使用者已在本機 .env 設定 Gmail SMTP，並明確授權寄送註冊／重設密碼測試信至指定信箱。帳密、收件地址與驗證碼不納入此紀錄。

## 結果

- Gmail SMTP 接受註冊與重設密碼兩封驗證信；使用者已確認收到兩封信，SMTP 接受與實際收件均已驗證。
- 透過既有 API 完成註冊、OTP 驗證、工作階段建立、重設 OTP、密碼重設及新密碼登入。
- 測試僅使用有主機／角色／伺服器標記守衛的 foodiemo_v2_test，外層交易已回滾並核對測試帳號未保留。正式資料庫未新增測試帳號。
- 47 項單元測試通過；後端已重新啟動，本機 8002 健康檢查 HTTP 200。

## 本次修正

Windows 電腦名稱包含非 ASCII 字元，Python SMTP 預設 EHLO 會發生 UnicodeEncodeError。改將本機主機名稱轉成 IDNA ASCII 後傳入 local_hostname。

Gmail 應用程式密碼常以空白分組顯示；僅對 smtp.gmail.com 移除 ASCII 分組空白，其他 SMTP 供應商的密碼保留原字串。新增兩項測試覆蓋此差異與中文主機名稱。

首次嘗試在 EHLO 階段失敗，未寄出信件；修正後才寄出上述兩封信。無額外 SMTP 帳密公開或 HTTP OTP 預覽端點。
