# v2 帳號與照片串接驗收紀錄（2026-09-10）

本批延續原版 HTML 與互動，完成 Email 帳號、六題偏好、照片紀錄、地點／會員標註、留言及 Demo 會員串接。月度回顧動畫／下載／歷史月份列表仍待討論，不在本批。Google 登入入口保留，OAuth 尚未設定與實作。

## 已完成與驗證

- 註冊後四位 OTP，驗證後進原六題偏好；登入、登出、忘記密碼、OTP 與一次性密碼重設串接 PostgreSQL。密碼使用 scrypt 雜湊；工作階段只存 HttpOnly Cookie，資料庫只存 Token 雜湊。錯誤 OTP 最多五次、十分鐘到期、重寄至少六十秒；登入有失敗次數限制。
- 身分取自伺服器 Cookie；原請求的 email 僅作一致性檢查。重設密碼撤銷既有工作階段。六題偏好保存版本及答案，可由 `/api/me` 讀回，預留未來推薦演算法使用。
- 快門相簿選完直接保存；編輯頁依原選取數字順序 Finish，可混合舊照片與新照片。重新開啟還原選取地點及好友。
- 地點選擇讀取 restaurant_rows；好友搜尋已驗證會員名稱，至少兩個字、最多十人，不公開 Email。這是會員標註，尚無好友邀請／接受系統。
- 照片只允許 JPEG／PNG／WebP、每張最多 8 MB、每篇 1–10 張；實際解碼後轉 JPEG、移除 EXIF。首頁超過 2 MB 的相簿照片先以原編輯流程的 canvas 縮圖方式處理；後端像素限制未提高。新紀錄僅作者可讀寫，被標註者不因此獲得私人照片權限。
- 首頁、日曆、限動及我的社群共用 records/photos；社群刪整篇刪除所有照片，限動保留單圖刪除。刪除最後一圖亦移除空紀錄。失敗上傳會撤銷新增檔案，移除引用後才清理檔案。
- 原留言 Post／Enter 保存；會員名稱與留言用安全文字顯示。個人名稱及頭像保存失敗不當作成功。
- 付款保留卡片及返回流程，卡片欄位唯讀、明示 Demo 不扣款。建立金額 0 的示意會員與交易；重送不重複開通。Demo 會員無到期日，不代表正式訂閱計費。

## 驗證證據

- `scripts/test.py`：45 項通過。
- `scripts/test.py --integration`：14 項通過；專用 foodiemo_v2_test／角色／主機埠／伺服器標記防誤寫。API 測試使用外層交易回滾。
- `tests/run_browser_features.py`：11 項通過。Microsoft Edge 真實表單搭配隔離 PostgreSQL，測試信只由注入 Mailer 寫到忽略的本機檔案，無公開 OTP 端點。結束刪除本輪 UUID 人工帳號及其檔案，未使用正式帳號作 fixture。最終案例清單見忽略的 `test-results/features.json`。
- `tests/browser.cjs`：36 項原三卡、下拉、詳情、地圖、首頁三頁排列／側滑／返回及真實 PostgreSQL 推薦通過；正式測項僅唯讀。
- 前端 64 個腳本區塊／檔案的 JavaScript 語法通過。
- `scripts/check_source.py`：37 份原始檔未被修改；有意串接的 HTML 原 `<style>` 逐頁相同；推薦頁靜態控制及 CSS 相同。推薦截圖已檢視。
- Python 套件仍有 Starlette/AnyIO 棄用警告。瀏覽器隔離伺服器偶見 Windows socket 關閉通知，已完成案例及退出碼正常；並非 JavaScript 例外。尚未進行手機實機驗收。

## 正式資料庫

變更前備份：`backups/20260910_075239`。已在新隔離資料庫完整還原，比對 Schema、逐表筆數及備份 SHA256。

`apply_existing.py` 在套用前再次核對備份與正式庫一致、版本為 0001、Email 無大小寫衝突，再以單一交易套用 `0002_accounts_records`。新增 users 的驗證／頭像欄位及五張帳號相關表；原表筆數保留，新增表的執行角色權限已核對。未建立正式測試帳號。

`0001_baseline` 歷史檔不覆寫。回復採備份還原至新隔離庫、核對後再另行處理切換，不自動刪除帳號或照片。

## 外部設定與範圍限制

使用者表示 SMTP 尚未設定。目前正式註冊／寄送驗證碼會清楚回報寄送未完成，不回傳 OTP、不假裝寄信成功。需在忽略的 `.env` 設定 SMTP_HOST、SMTP_PORT、SMTP_USERNAME、SMTP_PASSWORD、SMTP_FROM_EMAIL 後，重新啟動服務並由使用者指定測試收件人做實信驗收。現有 SMTP 實作為 STARTTLS（通常 587）。APP_SECRET 保存在忽略的設定檔，不提交。

社群「饗你推薦」、愛心、檢舉、共同編輯、通知仍維持原版示例／原狀態；本批未將這些擴成完整社群服務。餐廳推薦演算法未定，仍使用可替換 RotationProvider；不宣稱已完成個人化推薦。完整月度回顧與教學頁未新增。
