# 本批實作進度

2026-09-10：帳號、六題偏好、照片保存、地點選擇、會員標註、留言及 Demo 會員串接已完成。原始前端與主樣式保留，首頁排列與餐廳下拉不變。

正式庫已由 0001 升級至 0002_accounts_records、0003_record_likes，並於 2026-09-15 以新備份核對後升級至 0004_friend_requests，並於 2026-09-19 以新備份核對後升級至 0005_chat_messages；新增表均保留原資料。好友功能備份／隔離還原紀錄為 `backups/20260915_062353`，聊天室 migration 備份為 `backups/20260919_041430`；未在正式庫建立人工測試帳號。

49 項單元、15 項 PostgreSQL、34 項原介面／推薦回歸通過。完整新增功能瀏覽器驗證結果及範圍限制見 [本批驗收紀錄](test-report-2026-09-10.md)。

SMTP 已完成本機設定並由使用者確認註冊／重設驗證信實際送達。好友搜尋與邀請／批准流程已串接；好友列表提供收到的邀請批准／忽略與好友聊天室入口，聊天室只允許已接受好友互相收發訊息。紀錄仍僅作者私人存取。

月度回顧仍待討論；教學未新增；Google OAuth、原社群示例入口未擴充。

## 2026-09-15 好友關係

- 首頁中間好友圖示已開啟好友頁：可列出好友、以名稱／Email 搜尋已驗證使用者並送出請求。
- 收件者在右上角最新消息看到好友請求，可批准或忽略；批准後雙方的好友列表都會更新。
- 新增 `friend_requests` 表、待處理／已接受去重索引與 API；隔離 PostgreSQL 16 項測試、真實瀏覽器好友流程 3 項檢查通過。

## 2026-09-15 拍照留言與愛心

- 首頁拍照審核畫面新增淺色 `Add a message` 輸入欄；保存時以 `initial_comment` 同一交易建立照片紀錄與第一則留言。留言在貼文評論頁以一般留言顯示。
- 新增 `record_likes`（record_id、user_id 複合主鍵）及 POST／DELETE `/api/posts/{id}/like`；重複按讚冪等，取消後可再次按讚。我的社群貼文回傳 `likes`、`isLiked`，推薦示意卡維持本地示意互動。社群操作列現在只顯示愛心數字與留言數字，不再顯示 `likes` 文字。
- 驗證：來源保護、49 項單元、15 項隔離 PostgreSQL、34 項瀏覽器回歸，以及 11 項隔離真實瀏覽器流程通過；隧道瞬斷後重跑整合測試為 15/15。

## 2026-09-12 SMTP 更新

本機 SMTP 已完成設定，兩封驗證信獲 Gmail SMTP 接受；註冊、重設密碼與新密碼登入 API 測試通過，測試帳號已回滾。使用者已確認兩封信實際送達。修正中文電腦名稱及 Gmail 密碼分組空白問題；47 項單元通過，詳見 [SMTP 紀錄](smtp-test-2026-09-12.md)。

## 2026-09-19 好友互動與版本資訊

- 個人檔案頁顯示 v2.1.1 與最後更新時間 2026-09-19 13:27（台灣時間），供手機實機驗收辨識版本。
- 好友列表新增收到的好友邀請區塊，可直接批准／忽略；已建立好友可按「聊天」開啟私有聊天室。
- chat_messages migration 已正式套用，正式庫版本為 0005_chat_messages；正式套用前建立並驗證 backups/20260919_041430。
## 2026-09-19 入口載入與互動效能（v2.1.2）

- index.html 先驗證登入狀態；未登入時不顯示主殼層，直接回到登入頁。重新登入入口會清理本機 IndexedDB、Service Worker 快取與帳號檢視快取。
- 主殼層等待回憶、首頁、社群三個分頁完成首屏資料／頭像 ready 後才解除旋轉載入遮罩；相機權限提示不再阻塞整個主畫面。
- 功能頁加入 ready handshake，返回時保留已完成的 iframe，避免每次切回都重建頁面與重新抓取資料。
- 最新消息移除靜態 DEMO 通知，只保留真實好友邀請；沒有邀請時顯示空狀態。
- 驗證：來源保護、inline JavaScript 語法、34 項瀏覽器回歸、手機尺寸 navigation-touch 12 項與消息空狀態檢查通過。

## 2026-09-19 快速發文、會員框與返回快取

- 個人頁前往 Demo 付款改走主殼層的功能 iframe；付款取消或完成會回到原功能歷史，不重建首頁 home iframe，因此不再因個人頁／付款頁返回而重新要求相機權限。
- 主畫面登入者為有效會員時，左上頭像會顯示金色鑽石框；會員狀態仍以 `/api/me`／後端資料為準，不能只改本機旗標取得權限。
- 快門拍攝審核的 `Add a message` 左右加入標記朋友與標記地點按鈕。好友使用既有會員搜尋器；地點使用既有餐廳搜尋器，按下保留時與照片在同一次 API 請求保存。
- 右下角畫筆頁新增 `Add a message`，文字會以第一則留言保存；Finish 同時保存好友標記與地點。選取 JPEG 時會先讀照片 EXIF GPS，再進行 canvas 壓縮；若使用者保留照片 GPS，會以 `photo_location_text` 保存於既有 `records.location_text`，後端儲存後仍會移除原始 EXIF。
- 位置策略：照片 EXIF 是畫筆多張照片的優先來源；沒有 EXIF 時仍可手動選餐廳地點。瀏覽器即時位置尚未自動啟用，避免未經使用者操作觸發額外定位權限；若未來需要，可另加明確的「使用目前位置」按鈕。
- 驗證：inline JavaScript 語法通過、34 項原版瀏覽器回歸通過、49 項單元測試通過、隔離 PostgreSQL 真實瀏覽器流程的好友／標記／留言／會員／照片案例通過；伺服器 health、HTTP、HTTPS 與 SSH tunnel 仍正常。手機相機實機仍需由使用者重新測試返回與照片 EXIF。
## 2026-09-20 標記好友初始清單與快速畫筆切換

- 快門審核與右下角畫筆編輯共用好友選擇器；開啟時先列出目前已接受的好友，輸入文字仍可搜尋其他已驗證會員。
- 清除選擇、取消、完成改為目前配色的 46px 觸控按鈕，並在小螢幕保留三欄排列。
- 主殼層以 transition id 忽略快速往返產生的舊 iframe load／timeout 事件；新功能頁會顯示旋轉載入遮罩，完成後才顯示內容。
- 會員頭像框改為不旋轉的正方形金色框。

## 2026-09-20 社群標記好友預覽

- 我的社群貼文若有 `mentions`，圖片左下角顯示圓形人頭按鈕；推薦示意貼文沒有標記時不顯示。
- 點擊按鈕會以縮放／淡入動畫顯示小型標記名單；再次點擊、按 Escape 或點擊其他區域會收起。名稱以安全文字節點輸出，不解讀 HTML。
- 真實 PostgreSQL 瀏覽器流程已驗證標記好友顯示與收起互動。

## 2026-09-20 標記按鈕視覺微調

- 標記好友按鈕依 UI 調整移到貼文圖片右下角，圖示移除加號，只保留人頭輪廓；彈出名單動畫與收起互動不變。


## 被標記貼文進入 Memories（2026-09-20）
- /api/get_memories?scope=memories 或 scope=social 會回傳登入者自己的紀錄，加上 record_mentions 中標記該登入者的紀錄；首頁仍使用作者限定的預設 scope。我的社群使用 social scope。
- 回傳 isOwner、isTagged、canEdit 權限欄位；被標記者可讀取照片，但無法編輯或刪除作者貼文。
- Memories 限動檢視對被標記照片顯示右上角選單，提供「取消被標記」與「檢舉」；取消標記會刪除自己的 record_mentions 關係並重新整理回憶。檢舉目前為 Demo 提示，尚未建立管理端審核流程。
- /api/photos/{photo_id} 允許作者或該貼文被標記會員讀取圖片。

- 我的社群對被標記貼文同樣顯示右上角選單，僅提供「取消被標記」與「檢舉」；取消後該貼文會從 B 的社群與 Memories 移除。


## 我的社群狀態標籤與篩選（2026-09-20）
- 我的社群貼文維持相同結構；作者貼文顯示低調灰色「我的貼文」標籤。
- 被標記貼文顯示品牌色淡底「@ 標記了你」標籤，並在貼文左側加上細色線。
- 貼文上方提供「全部／我的貼文／標記我的」篩選，篩選只改變目前列表，不改變資料。

## 被標記貼文標籤微調（2026-09-20）
- 移除被標記貼文左側的直線，只保留品牌色淡底「@ 標記了你」標籤。

## 貼文標頭與留言載入微調（2026-09-20）
- 移除貼文標頭中的「@我的社群」文字，保留地點資訊與「我的貼文／@ 標記了你」狀態標籤。
- 留言頁面改為先顯示底部留言面板，Loading 只顯示在留言列表內，不再使用整頁載入遮罩。

## 留言面板閃爍修正（2026-09-20）
- 留言 iframe 在底部面板建立完成前保持隱藏，避免先顯示整頁留言內容再切換成面板。
- 面板建立後才顯示，Loading 仍只位於留言列表內。
